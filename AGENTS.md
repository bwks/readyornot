## Overview
This project is to check the status of services. More than just "is port open"
Detect is the service is running.

## Crates
- `ssh` | Is the SSH service is running.
- `http` | Is a HTTP service running.