[package]
name = "readyornot-http"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.13.2", features = ["rustls"], default-features = false }
tokio = { version = "1", features = ["rt"] }
